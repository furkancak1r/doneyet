appId: com.doneyet.appios
---
- tapOn:
    text: "^(Aç|Open)$"
    optional: true
    label: "Confirm the seed deep link if iOS asks for it"
- assertVisible:
    id: home-screen
- waitForAnimationToEnd
- takeScreenshot: ${LOCALE}/ipad-home.capture
